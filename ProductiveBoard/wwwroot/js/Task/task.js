function fillValues(title, desc, id, statusId, typeId) {
    $("#task-title").val(title);
    $("#task-desc").val(desc);
    $("#task-id").val(id);
    $("#status-id").val(statusId);
    $("#type-id").val(typeId);
}

function addTask() {
}

function updateTask() {
}